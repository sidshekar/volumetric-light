#version 430 core

in vec2 depth;

float zFar = 20;



void main()
{
	// gl_FragDepth = gl_FragCoord.z;
	//vec4 color = vec4(1.0f, 0.0f, 0.0f, 1.0f);

	//float fColor =  -(depth.x/depth.y )/zFar;
    //gl_FragColor = vec4(fColor,fColor,fColor,1);
}